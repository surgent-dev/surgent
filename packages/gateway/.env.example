# only used for local development
CLOUDFLARE_HYPERDRIVE_LOCAL_CONNECTION_STRING_HYPERDRIVE=postgres://surgent:password@localhost/surgent #local only, on prod hyperdrive uses its own internal connection string
ZEN_MODELS=xxx
POSTGRES_TYPE=pg
STAGE=dev