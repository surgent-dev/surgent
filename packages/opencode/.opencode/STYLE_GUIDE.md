# Surgent style (keep diffs small)

- Make **minimal** changes. Don’t reformat files you’re not logically changing.
- Preserve existing style in the touched file (quotes/spacing). **Never mix quote styles** in the same file.
- Don’t duplicate logic (e.g. don’t invalidate the same query twice).
- Don’t do “cleanup” refactors unless explicitly asked.
